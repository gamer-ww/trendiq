name: TrendIQ — Har Ghante Trends Fetch Karo

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  fetch-trends:
    runs-on: ubuntu-latest

    steps:
      - name: Code download karo
        uses: actions/checkout@v4

      - name: Python setup karo
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Libraries install karo
        run: pip install -r requirements.txt

      - name: Trends fetch karo
        env:
          SUPABASE_URL:    ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY:    ${{ secrets.SUPABASE_KEY }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
          NEWS_API_KEY:    ${{ secrets.NEWS_API_KEY }}
          GEMINI_API_KEY:  ${{ secrets.GEMINI_API_KEY }}
          RAPIDAPI_KEY:    ${{ secrets.RAPIDAPI_KEY }}
        run: python fetch_trends.py
```

---

## Paste Kaise Karo — Step by Step

**1.** GitHub pe apni repository kholo

**2.** **"Add file"** → **"Create new file"** pe click karo

**3.** File name wali jagah pe ye likho:
```
.github/workflows/fetch.yml
